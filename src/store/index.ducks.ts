import loading from './loading/loading.ducks';
import users from './users/users.ducks';
import auth from './auth/auth.ducks';
import url from './url/url.ducks';

const Reducers = {
  loading,
  users,
  auth,
  url,
};

export default Reducers;
